{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "ai-overseas-sales-v1/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$PRISMA_BASELINE\" = \"true\" ]; then npx prisma migrate resolve --applied 202609200001_init; fi && npx prisma migrate deploy && node dist/index.js'",
    "healthcheckPath": "/health/db",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
